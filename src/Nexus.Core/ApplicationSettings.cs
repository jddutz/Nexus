namespace Nexus.Core;

/// <summary>
/// Settings that describe the application hosted by the Nexus runtime.
/// </summary>
public sealed record ApplicationSettings(string ApplicationName, string ApplicationVersion);
